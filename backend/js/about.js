/**
 * About page module
 * Placeholder for future enhancements
 */

export function init() {
    const root = document.getElementById('aboutPageRoot');
    if (!root) return;

    // Intentionally empty (Flowbite template content will be added later)
}

