; Laragon MySQL / MariaDB over TCP/IP
; Copy to project root .env or config/.env (config/env.php reads root first)

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=cho_db
DB_CHARSET=utf8mb4

# Optional: Vite dev server port (must match vite.config.js)
VITE_PORT=5173
